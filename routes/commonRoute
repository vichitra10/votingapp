const express = require('express');
const router = express.Router();

const commonController = require('../controllers/commonController');

router.get('/askquestion',  commonController.generate );

module.exports = router;
